const requireModule = require('./utils/requireModule');

/**
 * NOTE: The exports in this file are considered public API.
 * Likely to be used in preset modules.
 */
module.exports = {
    requireModule,
};
